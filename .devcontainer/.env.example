# Garmin Connect IQ Development Environment Variables
# Copy this file to .env and fill in your values (optional)
#
# IMPORTANT: .env files are ignored by git for security
# These credentials are only used during container build to download device definitions
# If not provided, fallback SDK downloads will be used instead

# Your Garmin Connect IQ developer account credentials (optional)
# GARMIN_EMAIL=your-email@example.com
# GARMIN_PASSWORD=your-password

# Note: For GitHub Codespaces, set these as repository secrets instead:
# Settings → Secrets and variables → Codespaces → New repository secret
